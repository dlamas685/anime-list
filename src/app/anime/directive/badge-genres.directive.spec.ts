import { BadgeGenresDirective } from './badge-genres.directive';

describe('BadgeGenresDirective', () => {
  it('should create an instance', () => {
    const directive = new BadgeGenresDirective();
    expect(directive).toBeTruthy();
  });
});
